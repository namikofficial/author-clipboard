//! GAction registrations. Populated in T013+.

#![allow(dead_code, unused_imports, clippy::doc_markdown)]

/// Register GAction entries on the application.
/// Populated in PR 6 when the runtime loop is wired.
pub fn register() {
    // GAction registration is PR 6 work.
}
