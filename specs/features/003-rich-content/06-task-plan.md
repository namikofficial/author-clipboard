# Task Plan: Rich Content Completion

1. Correct rich picker-entry conversion. Verify with `cargo test -p author-clipboard-ui-gtk pages::clipboard`.
2. Resolve and clear image previews safely. Verify with `cargo test -p author-clipboard-ui-gtk widgets::preview`.
3. Render parsed file metadata and preserve open URIs. Verify with `cargo test -p author-clipboard-ui-gtk widgets::preview`.
4. Run formatting, shared tests, and UI crate checks.
5. Complete `08-review-checklist.md` with verification evidence.
